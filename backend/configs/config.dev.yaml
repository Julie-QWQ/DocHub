server:
  port: 8080
  mode: debug
  read_timeout: 10
  write_timeout: 10

database:
  host: localhost
  port: 5432
  user: study_upc
  password: study_upc_password
  dbname: study_upc
  max_open_conns: 10
  max_idle_conns: 5

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "dev-secret-key"
  expire_time: 24

oss:
  provider: minio
  endpoint: "localhost:9000"
  access_key: "minioadmin"
  secret_key: "minioadmin"
  bucket_name: "study-upc-dev"
  region: "us-east-1"
  use_ssl: false

log:
  level: debug
  filename: "logs/app.log"
  max_size: 100
  max_backups: 3
  max_age: 30
